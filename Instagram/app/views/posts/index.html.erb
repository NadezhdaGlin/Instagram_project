<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <p>
      <%= link_to (image_tag(post.image_url(:thumb)) if post.image?),post %>
    </p>

    <p>
      <%= post.description %>
    </p>
  <% end %>
</ul>

<%= link_to "Add new post", new_post_path %>
